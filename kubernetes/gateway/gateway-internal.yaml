apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-internal
  namespace: gateway
spec:
  gatewayClassName: cilium
  addresses:
    # NetBird-routed IP (10.60.0.0/24 via talos-1); reachable only over NetBird.
    - type: IPAddress
      value: "10.60.0.10"
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.internal.homelab0.xyz"
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-internal
      allowedRoutes:
        namespaces:
          from: All
